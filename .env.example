# Copy this file to `.env` (same folder as docker-compose.yml) and uncomment
# whatever you need to change. `.env` is gitignored, so a future `git pull`
# never conflicts with your choices here.

# Only needed if something else on your server already uses port 3000.
# SKYBOX_PORT=8088
